# Transformer基础

Transformer 是一种以注意力机制为核心的神经网络架构。它不依赖循环神经网络逐步处理序列，因此能够更高效地并行训练。

自注意力机制会根据 Query、Key 和 Value 计算序列中不同位置之间的关系。多头注意力会从多个表示空间学习不同类型的关系，然后将结果组合起来。

Transformer 通常还包含位置编码、残差连接、层归一化和前馈神经网络。编码器适合理解输入，解码器适合逐步生成内容，而不同模型会根据任务选择不同结构。