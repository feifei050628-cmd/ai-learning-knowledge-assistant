# PyTorch训练

PyTorch 是常用的深度学习框架。一个基本训练流程通常包括准备数据、定义模型、设置损失函数、创建优化器和执行训练循环。

在每一轮训练中，程序先通过模型得到预测结果，再计算损失。调用 loss.backward() 会根据计算图计算梯度，optimizer.step() 根据梯度更新参数，optimizer.zero_grad() 清除上一轮保存的梯度。

训练时模型通常调用 train() 进入训练模式，推理时调用 eval() 进入评估模式。使用 torch.no_grad() 可以在不计算梯度的情况下完成推理，从而减少显存和计算开销。